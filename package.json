{
  "name": "travelmap",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "web",
    "server",
    "remotion"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace web",
    "dev:server": "npm run dev --workspace server",
    "studio": "npm run dev --workspace remotion"
  }
}
